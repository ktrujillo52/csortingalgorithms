#!/usr/bin/python
#Bucket sort
#Initialize data
l = ['5', '3', '1', '4', '2']
print l
#Create buckets
elements = []	#Buckets created for length of l
k = 0
buckets = []
maximum = l[0]
for i in l:
	if (i >= maximum):
		maximum = i
	else:
		next
while (k < maximum):
	buckets.append(elements)
	k = k + 1
print buckets
for i in range(len(l)):
	buckets[int(l[i])].append(l[i])
for i in range(len(buckets)):
	for j in range(len(i)):
		sort.append(j)
print sort
